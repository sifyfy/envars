{
  "name": "envars",
  "version": "0.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build-debug": "cargo build",
    "build-release": "cargo build --release",
    "doc": "cargo doc",
    "format": "cargo fmt",
    "test": "cargo test",
    "watch": "chokidar \"src/*.rs\" \"src/**/*.rs\" -c \"node js/check.js\""
  },
  "author": "Siphilia",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0"
  }
}
